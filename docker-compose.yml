version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: coffee_bot_db
      POSTGRES_USER: ura_suda
      POSTGRES_PASSWORD: 9zUumXR45J
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  bot:
    build: .
    depends_on:
      - db
    env_file:
      - .env
    restart: unless-stopped

volumes:
  postgres_data: